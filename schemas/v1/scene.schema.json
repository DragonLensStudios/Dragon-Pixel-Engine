{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dragonpixel.dev/schemas/v1/scene.schema.json",
  "title": "Dragon Pixel Engine scene document",
  "type": "object",
  "additionalProperties": false,
  "required": ["format", "formatVersion", "sceneId", "entities"],
  "properties": {
    "$schema": { "type": "string", "format": "uri" },
    "format": { "const": "dpe.scene" },
    "formatVersion": { "const": 1 },
    "producerVersion": { "type": "string" },
    "sceneId": { "type": "string", "format": "uuid" },
    "name": { "type": "string" },
    "entities": {
      "type": "array",
      "items": { "$ref": "#/$defs/entity" }
    }
  },
  "$defs": {
    "entity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "parentId", "components"],
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "name": { "type": "string" },
        "parentId": {
          "oneOf": [
            { "type": "null" },
            { "type": "string", "format": "uuid" }
          ]
        },
        "components": {
          "type": "array",
          "items": { "$ref": "#/$defs/componentRecord" }
        }
      }
    },
    "componentRecord": {
      "type": "object",
      "required": ["typeId", "schemaVersion", "owner", "properties"],
      "properties": {
        "typeId": { "type": "string", "minLength": 1 },
        "schemaVersion": { "type": "integer", "minimum": 1 },
        "owner": { "enum": ["native", "managed", "unknown"] },
        "properties": { "type": "object" }
      },
      "additionalProperties": true
    }
  }
}

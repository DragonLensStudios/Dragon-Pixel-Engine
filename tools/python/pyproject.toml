[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "dragonpixel-tools"
version = "0.1.0"
description = "External Dragon Pixel Engine automation client"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["dragonpixel_tools*"]
